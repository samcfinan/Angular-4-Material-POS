import { ParseUserRolePipe } from './parse-user-role.pipe';

describe('ParseUserRolePipe', () => {
  it('create an instance', () => {
    const pipe = new ParseUserRolePipe();
    expect(pipe).toBeTruthy();
  });
});
